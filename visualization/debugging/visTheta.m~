%load forecast matrix
if ~exist('FM','var')
    load('WETSForecastMatrix')
    FM = WETSForecastMatrix.FM_subset;
    clear WETSForecastMatrix
end
mdpInputs

for t = 1:50
    theta_a(:,t) = theta(t,FM,1,mdp.mu,mdp.tp);
end

for i = 1:4
    plot(1:size(theta_a,2),theta_a